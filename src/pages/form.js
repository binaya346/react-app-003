import FormComponent from '../components/form';
import Navbar from "../components/navbar";

const Form = () => {
    return (
        <div>
            <Navbar />
            <h1>Form Page</h1>
            <FormComponent />
        </div>
    );
};

export default Form;        